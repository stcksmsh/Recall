# brain/ is its own separate, private git repo (github.com/stcksmsh/recall-brain) as of
# .ai/decisions/0009 — split out of this (public) repo so real personal data never lands in
# public history. Ignored here entirely, including its own derived index.
brain/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/

# Local model downloads / eval scratch
.cache/
models/
.env

# Eval run outputs
eval/results_*.json
